/*
 * BÀI 3: BÀI TOÁN XỬ LÝ PHƯƠNG TRÌNH BẬC NHẤT (2.0 Điểm)
 * 
 * Yêu cầu:
 * - Nhập vào từ bàn phím 2 hệ số a và b của phương trình bậc nhất: ax + b = 0 (giả định a != 0).
 * - Tính nghiệm: x = -b / a
 * - Xuất ra màn hình: 
 *     + Chuỗi phương trình dạng: ax + b = 0
 *     + Nghiệm x (làm tròn 2 chữ số thập phân).
 */

#include <stdio.h>

int main() {
    // Sinh viên viết mã nguồn tại đây
    
    return 0;
}
